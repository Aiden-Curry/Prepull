# P1C Warcraft Logs audit

The current implementation and acceptance evidence are in [the completion report](p1c-completion-report.md). The [site-mapping audit](p1c-wcl-site-audit.md) preserves the separate Vanilla/Fresh observations.

Era uses `vanilla.warcraftlogs.com/api/v2/client`, standard zones 2000?2006, and verified S0 partition 1. Fresh remains a separate unavailable production context. The server token endpoint is `www.warcraftlogs.com/oauth/token`. No staging deployment was performed.
