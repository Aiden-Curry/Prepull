import { test, expect } from "@playwright/test";
import { signIn, id, expectSafeError } from "./helpers";
const routes = (version:string,guild:string,raid:string) => [`/${version}/guilds`,`/${version}/guilds/new`,`/${version}/guilds/${guild}`,`/${version}/guilds/${guild}/roster`,`/${version}/guilds/${guild}/roster/import`,`/${version}/guilds/${guild}/raids`,`/${version}/guilds/${guild}/raids/new`,`/${version}/guilds/${guild}/raids/${raid}`,`/${version}/guilds/${guild}/settings`];
test("owner route matrix covers Era and TBC guild pages", async ({ page }) => { await signIn(page); for (const version of ["era", "tbc"]) for (const route of routes(version, id("E2E_GUILD_A_ID"), id("E2E_RAID_A_ID"))) { const response = await page.goto(route); expect(response?.status() ?? 0, route).toBeLessThan(500); await expectSafeError(page); } });
test("unknown and cross-guild identifiers do not expose private data", async ({ page }) => { await signIn(page); for (const route of [`/era/guilds/${id("E2E_GUILD_B_ID")}`, `/era/guilds/${id("E2E_GUILD_A_ID")}/raids/${id("E2E_RAID_B_ID")}`, "/era/guilds/00000000-0000-0000-0000-000000000000", "/era/guilds/a-not-a-version"]) { const response=await page.goto(route); expect(response?.status() ?? 0).toBeGreaterThanOrEqual(200); await expectSafeError(page); } });
