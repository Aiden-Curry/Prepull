import assert from "node:assert/strict";
import test from "node:test";
import { classifyMainAltRows, validateRelationshipGraph } from "../lib/guilds/main-alt.ts";
test("rejects self, direct, and multi-level relationship cycles",()=>{const ids=new Set(["a","b","c"]);assert.throws(()=>validateRelationshipGraph([{mainMemberId:"a",altMemberId:"a"}],ids),/self-main/);assert.throws(()=>validateRelationshipGraph([{mainMemberId:"a",altMemberId:"b"},{mainMemberId:"b",altMemberId:"a"}],ids),/main-alt-cycle/);assert.throws(()=>validateRelationshipGraph([{mainMemberId:"a",altMemberId:"b"},{mainMemberId:"b",altMemberId:"c"},{mainMemberId:"c",altMemberId:"a"}],ids),/main-alt-cycle/);});
test("rejects multiple mains and cross-guild references",()=>{const ids=new Set(["a","b","c"]);assert.throws(()=>validateRelationshipGraph([{mainMemberId:"a",altMemberId:"c"},{mainMemberId:"b",altMemberId:"c"}],ids),/multiple-mains/);assert.throws(()=>validateRelationshipGraph([{mainMemberId:"outside",altMemberId:"a"}],ids),/cross-guild-main/);});
test("classifies missing, duplicate, differing, and self-main conflicts safely",()=>{const conflicts=classifyMainAltRows([{row:2,name:"Alt",mainName:"Missing"},{row:3,name:"Other",mainName:"Main"},{row:4,name:"Main",mainName:"Main"}], [{id:"a",characterName:"Alt",active:true,mainMemberId:"old"},{id:"o",characterName:"Other",active:true,mainMemberId:"old-main"},{id:"m",characterName:"Main",active:false}]);assert.ok(conflicts.some((c)=>c.kind==="unresolved-main-name"));assert.ok(conflicts.some((c)=>c.kind==="existing-main-differs"));assert.ok(conflicts.some((c)=>c.kind==="self-main"));const duplicate=classifyMainAltRows([{row:2,name:"Alt",mainName:""},{row:3,name:"Alt",mainName:""}],[]);assert.ok(duplicate.some((c)=>c.kind==="duplicate-imported-character"));});
