export function SectionHeader({ eyebrow, title, description, link }: { eyebrow: string; title: string; description?: string; link?: string }) { return <div className="mb-7 flex items-end justify-between gap-5"><div><div className="eyebrow mb-3">{eyebrow}</div><h2 className="display text-3xl text-[var(--text)] md:text-4xl">{title}</h2>{description && <p className="mt-2 max-w-xl text-sm leading-6 text-[var(--muted)]">{description}</p>}</div>{link && <span className="hidden text-xs font-semibold text-[var(--primary-light)] sm:block">{link} <span aria-hidden="true">↗</span></span>}</div>; }
