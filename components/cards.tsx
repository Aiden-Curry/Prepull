import Link from "next/link";
import { ClassInfo, ContentVersion, Raid, Tool } from "../lib/types";
const tone: Record<string, string> = { gold: "bg-[#8f6a36]", green: "bg-[#3c9860]", purple: "bg-[#6d4bc3]", red: "bg-[#9a493a]", blue: "bg-[#397aa2]" };
export function RaidCard({ raid, version }: { raid: Raid; version: ContentVersion }) { return <Link href={`/${version}/raids`} className="panel group block min-w-[210px] rounded-xl p-4 transition duration-200 hover:-translate-y-1 hover:border-[var(--primary)]"><div className={`mb-8 grid h-9 w-9 place-items-center rounded-lg ${tone[raid.accent]} text-sm font-bold text-white/80`}>✦</div><h3 className="display text-xl text-[var(--text)]">{raid.name}</h3><p className="mt-1 text-xs text-[var(--muted)]">{raid.location}</p><div className="mt-5 flex gap-3 text-[10px] font-semibold uppercase tracking-wider text-[var(--primary-light)]"><span>Lvl {raid.level}</span><span>•</span><span>{raid.bosses} bosses</span></div><span className="mt-4 block text-xs text-[var(--muted)] opacity-0 transition group-hover:opacity-100">View guide →</span></Link>; }
export function ToolCard({ tool, version }: { tool: Tool; version: ContentVersion }) { return <Link href={`/${version}/tools`} className={`panel group rounded-xl p-5 transition hover:-translate-y-1 hover:border-[var(--primary)] ${tool.featured ? "border-[var(--primary)]/40" : ""}`}><div className="mb-8 flex items-start justify-between"><span className="grid h-10 w-10 place-items-center rounded-lg bg-[var(--surface-raised)] text-xl text-[var(--primary-light)]">{tool.icon}</span><span className="text-lg text-[var(--muted)] transition group-hover:text-[var(--primary-light)]">↗</span></div><h3 className="text-base font-semibold text-[var(--text)]">{tool.name}</h3><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{tool.description}</p></Link>; }
export function ClassCard({ info, version }: { info: ClassInfo; version: ContentVersion }) { return <Link href={`/${version}/classes`} className="panel flex items-center gap-3 rounded-xl p-3 transition hover:border-[var(--primary)]"><span className={`grid h-10 w-10 place-items-center rounded-lg ${tone[info.tone]} text-xs font-bold text-white/90`}>{info.initials}</span><span><strong className="block text-sm text-[var(--text)]">{info.name}</strong><small className="text-xs text-[var(--muted)]">{info.role}</small></span></Link>; }
