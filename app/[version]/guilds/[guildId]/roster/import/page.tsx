import Link from "next/link";
import { VersionShell } from "../../../../../../components/version-shell";
import type { ContentVersion } from "../../../../../../lib/types";
import { previewGuildCsv } from "../../../../../../lib/guilds/csv";
export default function ImportRosterPage({ params }: { params: { version: string; guildId: string } }) { const sample = previewGuildCsv("name,region,realm,class,level,faction,guildRank,mainName,preferredRole,notes\nAidy,eu,Firemaw,Warrior,60,Horde,Officer,,DPS,Ready for review"); return <VersionShell version={params.version as ContentVersion}><main className="mx-auto max-w-3xl px-5 py-16"><Link href={`/${params.version}/guilds/${params.guildId}/roster`} className="text-xs text-[var(--muted)]">← Roster</Link><div className="panel mt-8 rounded-2xl p-6"><p className="eyebrow">Roster import</p><h1 className="display mt-2 text-4xl">CSV preview</h1><p className="mt-3 text-sm text-[var(--muted)]">Supported columns: name, region, realm, class, level, faction, guildRank, mainName, preferredRole, notes.</p><p className="mt-6 text-sm">Development preview parser is ready: {sample.rows.length} row, {sample.issues.length} issues.</p><p className="mt-2 text-xs text-[var(--muted)]">Application uses explicit import transactions and marks absent members inactive; it never silently deletes history.</p></div></main></VersionShell>; }
