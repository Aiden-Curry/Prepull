import Link from "next/link";
import { notFound } from "next/navigation";
import { VersionShell } from "../../../../components/version-shell";
import { getCharacterProvider } from "../../../../lib/providers/factory";
import { CharacterProviderError } from "../../../../lib/providers/character-provider";
import { isContentVersion } from "../../../../lib/game-data";
import { saveCharacterAction } from "../../../../lib/characters/saved-actions";
import { requireAuthenticatedUser } from "../../../../lib/auth";
import { protectedRouteCallback } from "../../../../lib/auth-callback";
import type { CharacterRealmType, ContentVersion, Region, NormalizedCharacter } from "../../../../lib/types";

export default async function ConnectCharacterPage({ params, searchParams }: { params: Promise<{ version: string }>; searchParams: Promise<Record<string, string | string[] | undefined>> }) {
  const { version: rawVersion } = await params; if (!isContentVersion(rawVersion)) notFound(); const version = rawVersion as ContentVersion; const query = await searchParams; await requireAuthenticatedUser(protectedRouteCallback(version, "/characters/connect", query));
  const region = String(query.region ?? "eu") as Region; const realmType = String(query.realmType ?? "era") as CharacterRealmType; const realm = String(query.realm ?? ""); const name = String(query.name ?? "");
  let character: NormalizedCharacter | null = null; let message = "";
  if (query.search === "1") {
    try { character = await getCharacterProvider().findCharacter({ contentVersion: version, realmType, region, realm, characterName: name }); if (!character) message = "Character not found. Check the realm, region, and spelling."; }
    catch (error) { message = error instanceof CharacterProviderError && error.code === "UnsupportedRealmType" ? "Live Anniversary character profiles aren't available yet." : error instanceof CharacterProviderError ? error.message : "Character search is temporarily unavailable."; }
  }
  return <VersionShell version={version}><main className="mx-auto min-h-[calc(100vh-148px)] max-w-[980px] px-5 py-12 lg:px-8 lg:py-16"><Link href={`/${version}/dashboard`} className="text-xs text-[var(--muted)] hover:text-[var(--primary-light)]">← Back to dashboard</Link><div className="mt-10 max-w-2xl"><div className="eyebrow mb-3">Characters / Connect</div><h1 className="display text-5xl tracking-[-.04em]">Bring your character with you.</h1><p className="mt-4 text-sm leading-7 text-[var(--muted)]">Find a public character profile and save it to your PrePull account. Saving a character does not verify ownership.</p></div><form method="get" className="panel mt-10 grid gap-4 rounded-2xl p-6 sm:grid-cols-2"><input type="hidden" name="search" value="1" /><label className="text-xs text-[var(--muted)]">Region<select name="region" defaultValue={region} className="field mt-2 w-full"><option value="eu">Europe</option><option value="us">Americas</option></select></label><label className="text-xs text-[var(--muted)]">Realm ecosystem<select name="realmType" defaultValue={realmType} className="field mt-2 w-full"><option value="era">Era</option><option value="anniversary">Anniversary</option></select></label><label className="text-xs text-[var(--muted)]">Realm<input name="realm" defaultValue={realm} className="field mt-2 w-full" placeholder="Firemaw" required /></label><label className="text-xs text-[var(--muted)]">Character name<input name="name" defaultValue={name} className="field mt-2 w-full" placeholder="Aidy" required /></label><button className="button-primary sm:col-span-2" type="submit">Search character</button></form>{message && <div className="mt-5 rounded-xl border border-[var(--line)] bg-[var(--surface)] p-5 text-sm text-[var(--muted)]" role="status">{message}<div className="mt-3"><Link className="text-[var(--primary-light)]" href={`/${version}/characters/connect`}>Search again →</Link></div></div>}{character && <section className="panel mt-6 rounded-2xl p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="eyebrow">Saved character</div><h2 className="display mt-2 text-3xl">{character.name}</h2><p className="mt-1 text-sm text-[var(--muted)]">{character.realm} · {character.region.toUpperCase()} · {character.realmType === "anniversary" ? "Anniversary" : "Era"}</p></div><span className="rounded-full border border-[var(--line)] px-3 py-2 text-xs text-[var(--primary-light)]">{character.dataMeta?.isLive ? "Live profile" : "Test profile"}</span></div><dl className="mt-7 grid gap-4 text-sm sm:grid-cols-4"><Fact label="Class" value={character.class} /><Fact label="Level" value={String(character.level)} /><Fact label="Race" value={character.race} /><Fact label="Faction" value={character.faction} /></dl><div className="mt-7 flex flex-wrap gap-3"><form action={saveCharacterAction}><input type="hidden" name="contentVersion" value={version} /><input type="hidden" name="realmType" value={character.realmType} /><input type="hidden" name="region" value={character.region} /><input type="hidden" name="realm" value={character.realm} /><input type="hidden" name="characterName" value={character.name} /><button className="button-primary" type="submit">Add to PrePull</button></form><Link href={`/${version}/characters/connect`} className="button-secondary">Search again</Link></div></section>}</main></VersionShell>;
}
function Fact({ label, value }: { label: string; value: string }) { return <div><dt className="text-[10px] font-bold uppercase tracking-widest text-[var(--muted)]">{label}</dt><dd className="mt-1 font-semibold">{value}</dd></div>; }
