import type { EquipmentSlot } from "../types.ts";
import type { NormalizedItemMetadata } from "./types.ts";

export type ItemMetadataIssue = { severity: "error" | "warning"; code: string; itemId: number; message: string };
const slots: Record<EquipmentSlot, string[]> = { Head: ["HEAD"], Neck: ["NECK"], Shoulder: ["SHOULDER"], Back: ["BACK"], Chest: ["CHEST"], Wrist: ["WRIST"], Hands: ["HANDS"], Waist: ["WAIST"], Legs: ["LEGS"], Feet: ["FEET"], "Finger 1": ["FINGER"], "Finger 2": ["FINGER"], "Trinket 1": ["TRINKET"], "Trinket 2": ["TRINKET"], "Main Hand": ["WEAPON"], "Off Hand / Shield": ["WEAPON", "SHIELD"], "Ranged / Relic": ["RANGED", "RELIC"] };
export function metadataSlotCompatible(item: NormalizedItemMetadata, curatedSlot: EquipmentSlot): boolean { if (item.slot === curatedSlot) return true; if (curatedSlot === "Off Hand / Shield" && item.slot === "Main Hand" && (!item.inventoryType || item.inventoryType === "WEAPON")) return true; const aliases = slots[curatedSlot]; if (!aliases) return true; const normalized = item.slot.toUpperCase(); return aliases.some((alias) => normalized.includes(alias)) || item.weapon !== undefined && aliases.includes("WEAPON"); }
export function validateMetadataRecord(item: NormalizedItemMetadata): ItemMetadataIssue[] { const issues: ItemMetadataIssue[] = []; if (!item.itemId || !item.name) issues.push({ severity: "error", code: "missing-identity", itemId: item.itemId, message: "Metadata requires item ID and name." }); if (!item.provenance?.source || !item.provenance.verifiedAt) issues.push({ severity: "error", code: "missing-provenance", itemId: item.itemId, message: "Metadata provenance is incomplete." }); return issues; }
