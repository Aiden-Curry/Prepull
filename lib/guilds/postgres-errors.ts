import { GuildDomainError } from "./errors.ts";
export function mapPostgresError(error: unknown): GuildDomainError { const candidate = error as { code?: string; constraint?: string; detail?: string }; if (error instanceof GuildDomainError) return error; if (candidate.code === "23505") { if (candidate.constraint?.includes("signup")) return new GuildDomainError("DUPLICATE", "A signup already exists for this raid character."); if (candidate.constraint?.includes("claim")) return new GuildDomainError("DUPLICATE", "This character already has an active claim."); return new GuildDomainError("DUPLICATE", "The requested record already exists."); } if (candidate.code === "23503") return new GuildDomainError("CROSS_GUILD_REFERENCE", "The referenced guild record does not belong to this operation."); if (candidate.code === "40001" || candidate.code === "40P01") return new GuildDomainError("CONFLICT", "The operation conflicted with another update. Reload and try again."); if (candidate.code === "57P01" || candidate.code === "08000") return new GuildDomainError("UNAVAILABLE", "Guild storage is temporarily unavailable."); return new GuildDomainError("UNAVAILABLE", "Guild storage could not complete the operation."); }
